files:
  "/etc/cron.d/mycron":
    mode: "000644"
    owner: root
    group: root
    content: |
      HOME=/var/app/current
      * * * * * php refine minutely

commands:
  remove_old_cron:
    command: "rm -f /etc/cron.d/*.bak"
